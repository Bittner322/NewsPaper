<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.newspaper">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_icon_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/app_icon_new_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewsPaper"
        tools:targetApi="31">
        <activity
            android:name=".presentation.faq.FaqActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.splash_screen.SplashScreen"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.history.HistoryActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.favorite_articles.FavoriteArticleActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.full_article.FullArticleActivity"
            android:exported="true" />
        <activity
            android:name=".presentation.main_activity.MainActivity"
            android:exported="true"></activity>
    </application>

</manifest>